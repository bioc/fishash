% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_guidebender}
\alias{simulate_guidebender}
\title{Simulate guide count matrix based on a cellbender-like model}
\usage{
simulate_guidebender(
  n_guides,
  n_cells,
  moi,
  hurdle_prob,
  guide_infection_alpha = 1,
  d_sigma_guide = 0.5,
  endo_shape_sum = 1,
  endo_shape_flat = 0,
  d_mu_drop = log(10),
  d_sigma_drop = 0.5,
  d_mu_cell = log(60),
  d_sigma_cell = 0.5,
  rho_alpha = 0.5,
  rho_beta = 9.5,
  eps_alpha = 50,
  Phi_cell = 1,
  Phi_noise = 0,
  return_sparse_only = FALSE,
  median_per_cell = NULL,
  chunk_cells = NULL
)
}
\arguments{
\item{n_guides}{Number of guides. Must be strictly greater than 1.}

\item{n_cells}{Number of cells. Must be strictly greater than 1.}

\item{moi}{The pre-selection multiplicity of infection. The
Poisson-rate in the hurdle Poisson for the number of guides per
cell.}

\item{hurdle_prob}{The probability a cell has zero guides; the
hurdle in the hurdle-Poisson for the number of guides per cell.}

\item{guide_infection_alpha}{The guide infection relative
frequencies (\code{p_g}) is Dirichlet-distributed with shape
\code{guide_infection_alpha}.}

\item{d_sigma_guide}{The guide expression size factor
(\code{d_g^{guide}}) is lognormal with mean 0 and SD \code{d_sigma_guide}}

\item{endo_shape_sum}{The endogenouse noise guide frequencies
\verb{\\chi_g^a} is Dirichlet distributed; its shape parameters sum
to \code{endo_shape_sum * n_guides}.}

\item{endo_shape_flat}{If this is 1, then the endogenous noise
guide frequencies \verb{\\chi_g^a} are Dirichlet distributed with
constant shape parameter equal to \code{endo_shape_sum}. If this is
0, then the Dirichlet shape parameters are instead proportional
to \code{d_g^{guide} * p_g}, and sum to \code{endo_shape_sum * n_guides}.
If this is between 0 and 1, it interpolates between these 2
options.}

\item{d_mu_drop}{The droplet ambient size factor \code{d_n^{drop}} is
lognormal with location \code{d_mu_drop}.}

\item{d_sigma_drop}{The droplet ambient size factor \code{d_n^{drop}} is
lognormal with scale \code{d_sigma_drop}.}

\item{d_mu_cell}{The cell size factor \code{d_n^{cell}} is lognormal
with location \code{d_mu_cell}.}

\item{d_sigma_cell}{The cell size factor \code{d_n^{cell}} is lognormal
with scale \code{d_sigma_cell}.}

\item{rho_alpha}{Exogeneous noise frequency (i.e. from PCR
chimeras) is \code{Beta(rho_alpha, rho_beta)}.}

\item{rho_beta}{Exogeneous noise frequency (i.e. from PCR chimeras)
is \code{Beta(rho_alpha, rho_beta)}.}

\item{eps_alpha}{Droplet-specificy capture efficiency param
\verb{\\epsilon_n} is Gamma with mean 1 and shape \code{eps_alpha}}

\item{Phi_cell}{The negative binomial overdispersion of the signal
counts}

\item{Phi_noise}{The negative binomial overdispersion of the noise
counts}

\item{return_sparse_only}{Whether to return only the sparse
matrices of counts and assignments, or whether to also return
the dense matrices of Poisson rates and latent odds-ratios.}

\item{median_per_cell}{If non-null, the total number of counts is
rescaled so that this is the median per cell.}

\item{chunk_cells}{Simulate in chunks, with this many cells per
chunk.  This option can reduce memory usage, when used in
combination with \code{return_sparse_only=TRUE}. Must perfectly
divide \code{n_cells}, and be strictly greater than 1.}
}
\value{
A SummarizedExperiment, with the following assays:
\describe{
\item{ground_truth}{A logical matrix indicating which cells were infected
with which guides}
\item{counts}{The observed guide counts}
\item{counts_signal}{The counts coming from the true signal (i.e.
with noise counts subtracted).}
}
When \code{return_sparse_only} is \code{FALSE}, the following assays are also added:
\describe{
\item{lambd_signal}{The Poisson rate of the signal counts}
\item{lambd_noise}{The Poisson rate of the noise counts}
\item{latent_odds_ratio}{The true odds ratio of each cell-guide pair.}
}
}
\description{
Simulate guide count matrix based on a cellbender-like model
}
\examples{
set.seed(123)
sim <- simulate_guidebender(n_guides = 5, n_cells = 20,
                            moi = 0.2, hurdle_prob = 0.1)
}
