% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_guidebender2}
\alias{simulate_guidebender2}
\title{Alternative interface to \code{simulate_guidebender} that tries to have
more interpretable input parameters, in particular: Signal to Noise
Ratio, count per cell, and fraction of noise that is endogeneous vs
exogeneous. These replace the following parameters from
\code{simulate_guidebender} which should not be used with this function:
\code{d_mu_drop}, \code{d_mu_cell}, \code{rho_alpha}, \code{rho_beta}.}
\usage{
simulate_guidebender2(
  n_guides,
  n_cells,
  moi,
  hurdle_prob,
  snr,
  count_per_cell,
  frac_noise_endo,
  rho_sum = 10,
  d_sigma_drop = 0.5,
  d_sigma_cell = 0.5,
  d_sigma_guide = 0.5,
  use_median = TRUE,
  ...
)
}
\arguments{
\item{n_guides}{Number of guides}

\item{n_cells}{Number of cells}

\item{moi}{The pre-selection multiplicity of infection. The
Poisson-rate in the hurdle Poisson for the number of guides per
cell.}

\item{hurdle_prob}{The probability a cell has zero guides; the
hurdle in the hurdle-Poisson for the number of guides per cell.}

\item{snr}{Ratio of counts from signal to counts from noise}

\item{count_per_cell}{Median or mean counts per cell. \code{use_median}
determines whether this specifies the median or mean.}

\item{frac_noise_endo}{Fraction of the noise that is endogeneous
(ie background extracellular debris) as opposed to exogeneous
(ie PCR chimeras).}

\item{rho_sum}{Sum of \code{rho_alpha} and \code{rho_beta} from
\code{simulate_guidebender}. Smaller values imply greater
variability between cells for the fraction of their counts
coming from exogenous noise (PCR chimeras).}

\item{d_sigma_drop}{The droplet ambient size factor \code{d_n^{drop}} is
lognormal with scale \code{d_sigma_drop}.}

\item{d_sigma_cell}{The cell size factor \code{d_n^{cell}} is lognormal
with scale \code{d_sigma_cell}.}

\item{d_sigma_guide}{The guide expression size factor
(\code{d_g^{guide}}) is lognormal with mean 0 and SD \code{d_sigma_guide}}

\item{use_median}{Whether \code{counts_per_cell} refers to the median
(TRUE) or mean (FALSE).}

\item{...}{Additional arguments to pass to
\code{simulate_guidebender}. Note the following arguments must NOT
be specified: \code{d_mu_drop}, \code{d_mu_cell}, \code{rho_alpha}, \code{rho_beta}.}
}
\description{
Alternative interface to \code{simulate_guidebender} that tries to have
more interpretable input parameters, in particular: Signal to Noise
Ratio, count per cell, and fraction of noise that is endogeneous vs
exogeneous. These replace the following parameters from
\code{simulate_guidebender} which should not be used with this function:
\code{d_mu_drop}, \code{d_mu_cell}, \code{rho_alpha}, \code{rho_beta}.
}
